# 今日补漏清单 · 2026-08-17（周一）

> 目标：把 **8-15 周六没做的 3 件** + **一直拖着的 git 尾巴** 一次性清掉。
> 实查确认漏掉的：
> - `python-practice/file_io.py` 未写（周末事1）
> - `notes/jd-scan-2026-08-15.md` 未写（周末事2）
> - `python-practice/warmup.py` 未写（周末事3 前半）
> - git 仍压着：M `tech-blog-2026-08-14.md`、M `每日复盘_2026-08-14.md`、?? `每日规划_2026-08-15.md`（全未提交）
>
> 合计 **4 件，≤3.5h**，今天全清 → Week 1 语法真正闭环。
> **Week 2（llm-101 / rag-vs-finetune）顺延到 8-18 开，不挤今天。**

---

## ☑ 1 ｜ 写 file_io.py（文件读写 + 异常）· ≤1h

- **动作**：`python-practice/file_io.py`，用 `open()/with` 读 `var_types.py`，统计总行数 / 注释行数 / 关键字（如 `def`/`class`）命中数；全程 `try/except` 兜底"文件不存在"等异常。
- **产出物**：`python-practice/file_io.py`（可运行、带异常处理）
- **完成标准**：`python3 python-practice/file_io.py` 无报错并打印统计结果。
- **意义**：补上"文件IO + 异常"最后一块，Week1 语法才齐。

## ☑ 2 ｜ 写 warmup.py（语法串烧自测）· ≤1h

- **动作**：`python-practice/warmup.py`，把「变量 / 控制流 / 函数 / 容器 / 文件」5 块各写 1–2 个小例串一遍，跑通即过。
- **产出物**：`python-practice/warmup.py`（可运行）
- **完成标准**：脚本实跑退出码 0。
- **意义**：Week1 收口自测，确认语法真的能动手写。

## ☑ 3 ｜ 写 jd-scan-2026-08-15.md（JD 反向校准）· ≤1h

- **动作**：刷 BOSS直聘 / 拉勾 10 个「AI 应用开发 / Agent 开发」JD，摘录高频硬技能（Python / LLM API / Agent 框架 / RAG / 向量库），对照当前进度写差距清单。
- **产出物**：`notes/jd-scan-2026-08-15.md`（≥8 个 JD 高频要求 + 你的差距，两栏）
- **完成标准**：文件存在且含"高频要求"和"我的差距"两栏。
- **意义**：计划里"每天 30min 刷 JD"的落地产物，反向校准后面学什么。

## ☑ 4 ｜ git 一次性清账 · ≤10min

- **动作**：把所有积压 + 今天新产物一起提交推送：
  ```bash
  cd /Users/gemini/WorkBuddy/2026-08-12-08-55-50
  git add -A
  git commit -m "Week1补漏: file_io/warmup + jd扫描 + 复盘/博客/规划收尾"
  git push
  ```
- **产出物**：一次干净的 `git push`，工作树干净。
- **完成标准**：`git status` 无未提交项，远程能看到本次提交。

---

## 验收（今晚/明早对账）

| # | 交付物 | 怎么查 |
|---|---|---|
| 1 | `python-practice/file_io.py` | 文件存在 + `python3` 跑通 |
| 2 | `python-practice/warmup.py` | 文件存在 + 跑通 |
| 3 | `notes/jd-scan-2026-08-15.md` | 文件存在 + 两栏齐全 |
| 4 | git 干净 | `git status` 空 + 远程有提交 |

> 4 件全勾 = 今天胜利，Week1 正式闭环。明天 8-18 进 Week 2。
