# 每日早规划 · 2026-08-15（周六 · 周末选做版）

> 阶段定位：**阶段 1 第 1 周 ｜ Python 语法回炉**
> 本周目标：跟完《Python 快速上手》核心语法，验收 = 能独立写出 200 行数据处理脚本。
> 昨天（8-14 周五）状态：**3/3 已闭环**（实查：远程 HEAD=`46763d4`、data_tools.py 实跑通过、博客已 push）。**周五=本周收尾日，周末本该歇。**
> 今天定位：**周末选做版**——3 件保持手感的任务，做 1 件也算赢；不想动就踏实歇，周一进下一节语法。

---

## 一、开场

Nick，周六 9 点——本周五已经 3/3 收尾了，今天不逼你，给 3 件"手感保持"的轻量活，挑着做。

## 二、昨天进度（请你确认/补充）

> 以下是我实查工作区 + git 得出的判读，**以你回为准**。若有出入直接改。

| # | 昨天任务 | 客观证据 | 我的判读 | 你来确认 |
|---|---|---|---|---|
| 1 | 连 GitHub 远程并 push | 远程 HEAD=`46763d4`，`PUSH_EXIT=0`，凭据入钥匙串 | ✅ 完成 | ✅ / ❌ |
| 2 | 控制流+函数+容器练习 | `data_tools.py` 388 字节、`python3` 实跑退出码 0 | ✅ 完成 | ✅ / ❌ |
| 3 | 周五技术博客输出 | `notes/tech-blog-2026-08-14.md` 已写并 push | ✅ 完成 | ✅ / ❌ |

**小尾巴（非阻塞）**：`git status` 显示 2 个文件还没 commit——`tech-blog-2026-08-14.md`（昨晚又改了一版）、`每日复盘_2026-08-14.md`。不卡进度，今天顺手收掉即可（见事 3）。

> 直接回一句，例如：**"昨 3/3 没毛病，尾巴我等下收"** 或 **"事2其实没跑通，我重做"**。

## 三、今天 3 件具体事（周末轻量版，每件 ≤1.5h，都有交付物）

> 全部「选做」。Week1 语法已覆盖：环境 / 变量类型 / 控制流 / 函数 / 容器 / 列表推导 / GitHub。
> 还差「文件 IO + 异常」这两块才凑齐"200 行数据处理脚本"的能力——今天主攻它。

### ◯ 事 1 ｜ 文件读写 + 异常练习（≤1.5h）

- **动作**：写 `python-practice/file_io.py`，用 `open()/with` 读一个文件（可直接读 `python-practice/var_types.py` 或自建 `tasks.csv`），统计总行数 / 注释行数 / 按关键字计数；全程用 `try/except` 兜底文件不存在等异常。
- **产出物**：`python-practice/file_io.py`（可运行、带异常处理）
- **完成标准**：`python3 python-practice/file_io.py` 无报错并打印统计结果。
- **关联验收**：补上"文件IO + 异常"两块，朝"200 行数据处理脚本"再近一步。

### ◯ 事 2 ｜ JD 反向校准（计划里的"每天 30min 刷 JD"，≤1h）

- **动作**：刷 BOSS直聘 / 拉勾 10 个「AI 应用开发 / Agent 开发」JD，摘录高频硬技能（Python / LLM API / Agent 框架 / RAG / 向量库），对照你当前进度写一份差距清单。
- **产出物**：`notes/jd-scan-2026-08-15.md`（列 ≥8 个 JD 的高频要求 + 你的差距）
- **完成标准**：文件存在且含"高频要求"和"我的差距"两栏。

### ◯ 事 3 ｜ 语法串烧自测 + git 收尾（≤1h）

- **动作**：把本周学的「变量 / 控制流 / 函数 / 容器 / 文件」5 块，写 `python-practice/warmup.py` 用 8–10 个小例串一遍，跑通即过；顺手把昨晚 2 个未提交文件一起 `git add -A && git commit -m "..." && git push` 收掉（2 分钟仪式，push 已免令牌）。
- **产出物**：`python-practice/warmup.py` + 一次干净的 `git push`
- **完成标准**：脚本实跑通过 + 远程能看到本次提交，工作树干净。

---

## 四、认领确认

回我一句就当认了，比如：
- **"今天只做 1，2、3 周末再说"**
- **"3 件都挑着做"**
- **"今天歇，周一见"**

认完我先不催——你按自己节奏来，明早（或周一）对账。底线：本周已经收尾了，**今天没有任何"必须做"**，动一动只是为了别把手感凉了。
