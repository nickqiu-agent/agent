# 每日早规划 · 2026-08-17（周一 · Week 2 开题）

> **阶段判定**：阶段 1 第 **2** 周 ｜ **LLM 原理扫盲**（计划表：Week2 = Token / 上下文 / 温度 / Embedding / RAG / 微调区别；验收 = 能讲清 RAG vs 微调）
> **实查现状**：Week 1 语法 90% 完成（环境✅ / 变量✅ / 控制流·函数·容器✅ data_tools.py / GitHub push✅ / 周五博客✅），但**尾巴没收**——`file_io.py`（文件IO+异常）未写；git 还有 2 改 1 未跟踪未提交。
> **今天定位**：先用 1 小时收掉 Week1 尾巴 → 全力开 Week 2。今天是周一，新的一周从 0 动起来。

---

## 一、开场

Nick，周一 9 点——Week 1 语法基本收了，今天进 Week 2（LLM 原理），先把上个周末没动的那点尾巴 1 小时清掉，然后今天给你 3 件能交付的活。

---

## 二、昨天（8-16 周日）进度 —— 请你确认/补充

> 我实查工作区 + git：`python-practice/` 里**没有** `file_io.py`、`warmup.py`，git 仍是 `M tech-blog` / `M 复盘8-14` / `?? 规划8-15` 三处未提交。判读 = **周末没动代码、尾巴原样留着**。以你回为准。

| # | 我猜的昨天状态 | 客观证据 | 你来确认 |
|---|---|---|---|
| 1 | 周末歇了，没碰 file_io | 工作区无 file_io.py | ✅ 歇了 / ❌ 其实动了 |
| 2 | 没收 git 尾巴 | `git status` 仍 2改1未跟踪 | ✅ 没管 / ❌ 已提交 |

**直接回一句**，例如：**"周日歇了，尾巴还在，今天清"** 或 **"我其实写了 file_io，你没看到"**。

---

## 三、今天 3 件具体事（≤2h/件，都有交付物，无"学XX"）

> 节奏：事1 是 1 小时收尾（清账），事2、事3 是 Week 2 真正的开题件。Week 2 验收 = 能讲清 RAG vs 微调，今天把地基（概念）+ 验收件初稿都支起来。

### ✅ 事 1 ｜ 收掉 Week1 尾巴：file_io.py + git 清账（≤1h）

- **动作**：写 `python-practice/file_io.py`——用 `open()/with` 读 `var_types.py` 或自建 `tasks.csv`，统计总行数 / 注释行数 / 关键字命中数；全程 `try/except` 兜底"文件不存在"等异常。跑通后，把积压的 2 改 1 未跟踪一起 `git add -A && git commit -m "Week1收尾: file_io + 复盘/博客收尾" && git push`。
- **产出物**：`python-practice/file_io.py`（可运行、带异常处理）+ 一次干净的 `git push`（工作树干净）
- **完成标准**：`python3 python-practice/file_io.py` 无报错并打印统计；远程能看到本次提交。
- **意义**：把"文件IO + 异常"补上，Week 1 语法才真正闭环（够格写 200 行数据处理脚本）。

### ✅ 事 2 ｜ Week2 开题·基础概念笔记（≤2h）

- **动作**：写 `notes/llm-101-2026-08-17.md`，用**自己的话 + 类比 + 最小例子**讲清这 4 个基础概念：① Token（分词，中英文差异）② 上下文窗口（为什么长文会"忘"）③ 温度 temperature（控随机性，什么场景调高/低）④ Embedding（文本→向量，为什么能算相似度）。
- **产出物**：`notes/llm-101-2026-08-17.md`（4 个概念，每个含"一句话定义 + 类比 + 例子/数字"）
- **完成标准**：文件存在、4 个概念齐全、不是抄书——是你能对着人讲明白的版本。
- **意义**：Week2 计划前 4 块（Token/上下文/温度/Embedding）今天一次拿下，为 RAG/微调打底。

### ✅ 事 3 ｜ Week2 验收件种子：RAG vs 微调 对比稿（初稿，≤2h）

- **动作**：起草 `notes/rag-vs-finetune-2026-08-17.md`，搭一张 **5 维对比表**——适用场景 / 成本 / 数据要求 / 更新方式 / 风险，每格先填你目前的理解（不确定的标"待查"），最后写一段"面试时能讲 3 分钟"的结论（结合你 PM 视角：什么项目该用哪个）。
- **产出物**：`notes/rag-vs-finetune-2026-08-17.md`（5 维表 + 你的结论段）
- **完成标准**：表 5 格都有内容（允许部分"待查"），结论段能说清"何时选 RAG、何时选微调"。
- **意义**：这就是 Week2 验收件（"能讲清 RAG vs 微调"）的初稿，本周内打磨成终稿，直接当面试弹药。

---

## 四、认领确认

回我一句就当认了，例如：
- **"3 件全认，今天干"**
- **"先做 1 和 2，3 明天"**
- **"今天只清尾巴(事1)，Week2 明天正式开"**

认完我就记上。底线：事1 这 1 小时今天必须落（清账不欠），事2/事3 是本周主线，先动哪个都行——**关键是今天别空转**。

> 明早 9 点我会对账：重点看 `file_io.py` 是否开张、git 是否干净、两个 notes 文件是否落地。
